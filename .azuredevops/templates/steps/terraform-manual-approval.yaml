parameters:
  - name: NotificationEmails
    type: string
    default: ""

steps:

  - task: ManualValidation@0
    name: "ManualApproval"
    timeoutInMinutes: 60
    inputs:
      onTimeout: reject
      notifyUsers: ${{ parameters.NotificationEmails }}
      instructions: 'Please validate the Terraform Plan stage before continuing'